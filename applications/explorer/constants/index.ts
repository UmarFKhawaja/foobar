import {
  AlignType,
  AnchorType,
  ButtonType,
  ButtonSize,
  ButtonVariant,
  GapSize,
  JustifyType,
  TextVariant
} from '../types';

export const DEFAULT_ALIGN_TYPE: AlignType = 'center';

export const DEFAULT_ANCHOR_TYPE: AnchorType = 'all';

export const DEFAULT_BUTTON_TYPE: ButtonType = 'primary';

export const DEFAULT_BUTTON_SIZE: ButtonSize = 'md';

export const DEFAULT_BUTTON_VARIANT: ButtonVariant = 'filled';

export const DEFAULT_GAP_SIZE: GapSize = 'none';

export const DEFAULT_JUSTIFY_TYPE: JustifyType = 'center';

export const DEFAULT_TEXT_VARIANT: TextVariant = 'body';

export * from './theme';
